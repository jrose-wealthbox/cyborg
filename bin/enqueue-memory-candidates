#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path("../lib", __dir__)
require "cyborg/memory_candidates"

exit Cyborg::MemoryCandidates::EnqueueCLI.new(harness: ARGV.fetch(0, "")).run
