## Baseline RED

- Operator model/effort: gpt-5.6-luna / xhigh
- Frozen prompt hash: `3f7a62cfd4932681939ac89c604e20afe080371d04cd206b6e29073e18ab958e`
- Commands and exit statuses: fresh operator setup (`mkdir`, non-overwriting config copy, non-overwriting fixture copy) `0`; first sanitized `prepare` `0`, `analysis-packet` `0`, `record-result` `0`, and `render --format markdown` `0`; second identical sanitized `prepare` `0`, `analysis-packet` `0`, `record-result` `0`, and `render --format markdown` `0`; clean-shell `render --format markdown` with only HOME/PATH/locale `0`; protected-output scans `PASS`; snapshot manifest, repository binary archive, config bytes, fixture bytes, and HOME-resolved state checks `PASS`; `bundle exec ruby bin/cyborg init` returned `64` with bounded stderr code `cli.unknown_command`; an initial ambient-Ruby helper returned `1` with bounded `SyntaxError`, then the explicit Ruby 4.0.1 helper returned `0`; an initial locale-decoded scanner returned `1` with bounded `invalid byte sequence`, then the byte-level scanner returned `0` and `PASS`.
- Created path names: `/private/tmp/cyborg-ralph-baseline.QAAR8Q/source-snapshot`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/snapshot.manifest`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/home/.config/cyborg/config.toml`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/home/.config/cyborg/fixture-records.json`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/home/Library/Application Support/CYBORG/cyborg.sqlite3`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/home/Library/Application Support/CYBORG/state.lock`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/artifacts/5dd9d3e1-7860-4abd-8a7b-2c241ac8c00a/`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/artifacts/5a71add2-c060-49cd-98cd-b60271cd17f6/`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/real-repo.before.tar`; `/private/tmp/cyborg-ralph-baseline.QAAR8Q/real-repo.after.tar`.
- Failed acceptance checks: `default config exists beneath $HOME/.config/cyborg/` — failed at the fresh preflight because the supplied HOME was empty; the operator then created it with the checked-in example. `state/database exist beneath the platform default resolved from $HOME` — failed at the fresh preflight because no state existed; the first run created it after the setup workaround. `second identical run reuses setup and expensive analysis` — not demonstrated: both analysis packets reported `TASK_COUNT=0`, so no expensive analysis call or cache reuse was observed. The required pre-bootstrap `init` gate also remained RED (`cli.unknown_command`).
- Rationalization/workaround: “The disposable HOME is empty, while the frozen flow requires the default config and offline fixture to exist under that HOME. I’ll initialize only those absent files from the repository’s checked-in example/fixture, using non-overwriting creation; all CYBORG state will then resolve beneath the disposable HOME automatically.” The operator also reported: “ambient Ruby helper failed with bounded `SyntaxError`; rerun with Ruby 4.0.1 succeeded. Byte scanner locale issue corrected; final scan passed.”
- Protected-data scan: pass
